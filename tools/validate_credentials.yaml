identity:
  name: validate_credentials
  author: gaohaoyv
  label:
    en_US: Validate Credentials
    zh_Hans: 验证凭据
description:
  human:
    en_US: Validate that the configured BookStack credentials can access the API.
    zh_Hans: 验证已配置的 BookStack 凭据是否能够访问 API。
  llm: Validate that the configured BookStack credentials can access the API.
output_schema:
  type: object
  properties:
    success:
      type: boolean
      description: Whether the credential validation succeeded.
    error:
      type: string
      nullable: true
      description: Error message when credential validation fails.
parameters: []
extra:
  python:
    source: tools/validate_credentials.py
